import { OrganisationsRelation } from './organisations-relation';

describe('OrganisationsRelation', () => {
  it('should create an instance', () => {
    expect(new OrganisationsRelation()).toBeTruthy();
  });
});
